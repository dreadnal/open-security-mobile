import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ReconnectPage } from './reconnect';

@NgModule({
  declarations: [
    ReconnectPage,
  ],
  imports: [
    IonicPageModule.forChild(ReconnectPage),
  ],
})
export class ReconnectPageModule {}
